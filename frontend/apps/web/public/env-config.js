// Placeholder for local dev. In production this file is generated at
// container startup by nginx's envsubst from apps/web/env-config.js.template,
// so the same built image can be reused across environments.
window.__RUNTIME_CONFIG__ = {};
